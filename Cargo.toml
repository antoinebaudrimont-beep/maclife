[package]
name = "maclife"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
description = "Read-only X11 application and window identity probe"
license = "MIT"

[dependencies]
x11rb = { version = "0.14.0", default-features = false, features = ["allow-unsafe-code"] }

