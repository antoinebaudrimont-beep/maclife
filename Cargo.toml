[package]
name = "maclife"
version = "0.1.0"
edition = "2021"
rust-version = "1.85"
description = "Mac-style X11 application lifecycle daemon"
license = "MIT"

[dependencies]
libc = "0.2"
zbus = "4.4.0"
x11rb = { version = "0.14.0", default-features = false, features = ["xinput", "xtest"] }
